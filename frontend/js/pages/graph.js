// Graph page wrapper. The observatory owns its canvas lifecycle; this module just
// adapts it to the router's { mount, unmount } contract.

export { mount, unmount } from '../graph/observatory.js';
